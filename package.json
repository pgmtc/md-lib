{
  "name": "md-lib",
  "version": "1.0.25",
  "description": "Dependency library for modular desktop portlets",
  "scripts": {
    "test": "standard",
    "standard": "standard --fix",
    "prepublish": "npm run build",
    "start": "babel -s inline -d lib -w src",
    "build": "babel -s inline -d lib src"
  },
  "author": "praguematica",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-function-bind": "^6.22.0",
    "babel-preset-es2015": "^6.24.1",
    "standard": "^11.0.1"
  },
  "dependencies": {
    "winston": "^2.4.2"
  }
}
